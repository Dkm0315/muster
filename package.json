{
  "name": "muster",
  "version": "0.0.1",
  "private": true,
  "description": "Independent pi-native adaptive AI harness runtime.",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm --filter @musterhq/core build && pnpm --filter @musterhq/core typecheck && pnpm --filter @musterhq/cli typecheck && pnpm --filter @musterhq/ui typecheck",
    "test": "pnpm -r test",
    "hc": "tsx packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  }
}
