{
  "name": "muster",
  "version": "0.1.0",
  "private": true,
  "description": "Independent pi-native adaptive AI harness runtime.",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm --filter @dkm0315/core build && pnpm --filter @dkm0315/gateway build && pnpm --filter @dkm0315/core typecheck && pnpm --filter @dkm0315/gateway typecheck && pnpm --filter @dkm0315/cli typecheck && pnpm --filter @dkm0315/ui typecheck",
    "test": "pnpm -r test",
    "hc": "tsx packages/cli/src/index.ts",
    "portal:data": "node scripts/generate-portal-data.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  },
  "license": "MIT"
}
